name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: ./Scripts/build-debug.sh
      - name: Test
        run: ./Scripts/test.sh
